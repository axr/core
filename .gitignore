# Xcode
.DS_Store
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.xcworkspace
!default.xcworkspace
xcuserdata
profile
*.moved-aside
DerivedData
.idea/

*-debug/
*-release/

# CMakeLists.txt prevents in-source builds, but the process will still get far
# enough such that a few CMake files are introduced into the source directory
CMakeCache.txt
CMakeFiles

# In-source doxygen builds
src/core/docs

# Visual Studio creates these as part of the resource build process
*.aps
